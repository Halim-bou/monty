#include "monty.h"

/**
 * nop - do nothing
 * @stack: the given stack
 * @line_num: number of line
 * Return: Nothing
 */
void nop(stack_t **stack, unsigned int line_num)
{
	(void) stack;
	(void) line_num;
}
